{
    "//": "Shared Windows Terminal preferences, independent of theme and machine. apply-theme.py merges THIS file + the machine's theme (themes.json, picked by machines.json) into the live settings.json, preserving each machine's own profile list (GUIDs). Do not put colorScheme/font/opacity here (that belongs to the theme) nor the profile list (that belongs to the machine).",
    "copyFormatting": "none",
    "copyOnSelect": false,
    "keybindings":
    [
        { "id": "Terminal.CopyToClipboard", "keys": "ctrl+c" },
        { "id": "Terminal.PasteFromClipboard", "keys": "ctrl+v" },
        { "id": "Terminal.DuplicatePaneAuto", "keys": "alt+shift+d" }
    ],
    "newTabMenu": [ { "type": "remainingProfiles" } ],
    "theme": "system",
    "useAcrylicInTabRow": true,
    "defaults":
    {
        "cursorShape": "filledBox",
        "padding": "12,10,12,10",
        "scrollbarState": "hidden",
        "useAcrylic": true
    }
}
